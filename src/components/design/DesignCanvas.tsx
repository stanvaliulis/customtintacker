/**
 * @deprecated DesignCanvas has been consolidated into DesignEditor.
 *
 * DesignEditor now directly manages the Fabric.js canvas through
 * useDesignCanvas, including auto-fit zoom, pan/scroll, snap guides,
 * and a checkered background. There is no need for a separate
 * DesignCanvas component.
 *
 * This file is kept as a stub to avoid broken imports during migration.
 */

'use client';

export default function DesignCanvas() {
  return null;
}
